console.clear();

let base = 5;

// all your base are belong to us
console.log("====================");
console.log(` Tabla del ${base} `);
console.log("====================");

for (let i = 0; i <= 10; i++) {
    console.log(`${base} * ${i} = ${base * i}`);
}